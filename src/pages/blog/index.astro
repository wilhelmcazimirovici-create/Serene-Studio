---
import BaseLayout from "@/layouts/BaseLayout.astro";
import BookingCta from "@/components/BookingCta.astro";
import { getBlogPosts } from "@/data/blog";
import { absoluteUrl } from "@/data/site";

const posts = getBlogPosts("ro");
const path = "/blog/";
const title = "Blog Serene Studio - ghiduri cazare Bucuresti";
const description = "Articole practice despre cazare langa Piata Muncii, Arena Nationala, spitale, Parcul IOR si Sector 2 Bucuresti.";
const schema = [
  {
    "@type": "BreadcrumbList",
    itemListElement: [
      { "@type": "ListItem", position: 1, name: "Acasa", item: absoluteUrl("/") },
      { "@type": "ListItem", position: 2, name: "Blog", item: absoluteUrl(path) }
    ]
  }
];
---

<BaseLayout locale="ro" path={path} translationPath="/en/blog/" title={title} description={description} keyword="blog cazare Bucuresti" schema={schema}>
  <section class="hero">
    <div class="container">
      <p class="eyebrow">SEO local · Piata Muncii · Sector 2</p>
      <h1>Blog Serene Studio</h1>
      <p class="hero-lead">{description}</p>
      <div class="hero-actions">
        <BookingCta locale="ro" referer="website_blog_index_hero" label="Verifica disponibilitatea" />
      </div>
    </div>
  </section>
  <section class="band alt">
    <div class="container cards-grid">
      {posts.map((post) => (
        <article class="card">
          <h2><a href={`/blog/${post.slug}/`}>{post.title}</a></h2>
          <p>{post.description}</p>
          <a class="btn-soft" href={`/blog/${post.slug}/`}>Citeste articolul</a>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>
